services:
  jade_api:
    image: gleidsoncosta/jade-api-old:v2.0.0
    container_name: jade_api
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ./entrypoint.sh
    ports:
      - "8000:8000"
